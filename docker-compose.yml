# ──────────────────────────────────────────────────────────
# LinyaShare - Docker Compose (Production)
# ──────────────────────────────────────────────────────────
# Usage:
#   docker compose up -d
#   docker compose logs -f
#   docker compose down
# ──────────────────────────────────────────────────────────

services:
  linyashare:
    build:
      context: .
      dockerfile: Dockerfile
      # Build-arg must match the runtime DATABASE_PROVIDER
      # (it determines which Prisma engine is baked into the image).
      #   sqlite (default) | mysql | postgres
      args:
        DATABASE_PROVIDER: ${DATABASE_PROVIDER:-sqlite}
    container_name: linyashare
    restart: unless-stopped
    ports:
      # Keep the app private by default; expose it through a trusted reverse proxy.
      - "127.0.0.1:3000:3000"
    environment:
      # ── Database ──
      # DATABASE_PROVIDER: sqlite (default) | mysql | postgres
      - DATABASE_PROVIDER=${DATABASE_PROVIDER:-sqlite}
      # Leave empty for the built-in SQLite database.
      # External examples:
      #   MySQL:       mysql://user:password@dbhost:3306/linyashare
      #   PostgreSQL:  postgresql://user:password@dbhost:5432/linyashare
      - DATABASE_URL=${DATABASE_URL:-}
      
      # ── Authentication ──
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:?NEXTAUTH_SECRET must be set}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:3000}
      
      # ── App Configuration ──
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
      - UPLOAD_DIR=/app/data/uploads
      - IMPORT_DIR=/app/data/import
      - GLOBAL_UPLOAD_DIR=/app/data/uploads/global
      
      # ── Node ──
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - TRUSTED_PROXY=${TRUSTED_PROXY:-false}
    volumes:
      # Persist data in local project directory
      - type: bind
        source: ./prisma
        target: /app/prisma
      - type: bind
        source: ./data
        target: /app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/setup"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

